{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "watchPatterns": [
            "monolith/**"
        ]
    },
    "deploy": {
        "startCommand": "cd monolith && gunicorn --bind 0.0.0.0:$PORT app:app"
    }
}